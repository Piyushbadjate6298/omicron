import ServicesGrid from '../../components/sections/ServicesGrid'
import { ArrowRight, Check } from 'lucide-react'
import './Services.css'
export default function Services({onInquiry}){return <div className="page"><section className="page-hero service-hero"><div className="container"><div className="eyebrow">02 / Services</div><h1>Designed around <span>how you actually travel.</span></h1><p>Outstation, airport, city, hotel and event journeys from a fleet built to scale from one car to a full group.</p></div></section><ServicesGrid onInquiry={onInquiry}/><section className="section service-detail"><div className="container service-detail-grid"><div><div className="eyebrow">A smoother process</div><h2 className="section-title">From brief to booking without the <span className="accent">friction.</span></h2></div><div className="process-list">{['Share route, date and passenger count','Choose a suitable vehicle class','Receive availability and final quote','Confirm with the Omicron booking desk'].map((s,i)=><div key={s}><span>0{i+1}</span><Check size={17}/><b>{s}</b></div>)}<button className="btn btn-dark" onClick={()=>onInquiry()}>Start a request <ArrowRight size={17}/></button></div></div></section></div>}
