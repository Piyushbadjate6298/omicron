import { ShieldCheck, CircleDollarSign, Headphones, Sparkles } from 'lucide-react'
import { stats } from '../../data/fleet'
import './Trust.css'
export default function Trust(){const items=[[ShieldCheck,'Safety first'],[CircleDollarSign,'Transparent pricing'],[Headphones,'24 × 7 support'],[Sparkles,'Premium fleet']];return <section className="section trust-section dark"><div className="container"><div className="trust-top"><div><div className="eyebrow">Proof, not noise</div><h2 className="section-title">A trusted travel partner, made easier to choose.</h2></div><p className="section-copy">The numbers and promise points below are drawn from the supplied Omicron Journeys material and recast into a clearer premium layout.</p></div><div className="stats-grid">{stats.map(s=><div key={s.label}><strong>{s.value}</strong><span>{s.label}</span></div>)}</div><div className="promise-grid">{items.map(([Icon,text])=><div key={text}><Icon size={22}/><span>{text}</span></div>)}</div></div></section>}
